window.onload = () => {
    let mySwiper = new Swiper(".swiper-container", {
        loop: true,
        pagination: {
            el: '.swiper-pagination',
        },
    })

    let bscroll = new BScroll(".bscroll")
}